.ui.segment.condition_fields.ransack_fields{ 'data-object-name' => f.object_name }


  .inline.fields
    .fourteen.wide.field
      = f.attribute_fields do |a|
        = render 'attribute_fields', f: a

      .field
        %label Predicate
        = f.predicate_select

      = f.value_fields do |v|
        = render 'value_fields', f: v

    .two.wide.field= button_to_remove_fields

  -#= button_to_add_fields(f, :value)